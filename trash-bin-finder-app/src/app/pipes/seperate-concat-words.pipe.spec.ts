import { SeperateConcatWordsPipe } from './seperate-concat-words.pipe';

describe('SeperateConcatWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new SeperateConcatWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
