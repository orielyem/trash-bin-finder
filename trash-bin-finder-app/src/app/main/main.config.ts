import { InjectionToken } from '@angular/core';

export const MAP_CONFIG = new InjectionToken('map.config');
export const SIDENAV_CONFIG = new InjectionToken('sidenav.config');
export const SERVER_CONFIG = new InjectionToken('server.config');
export const STATUS_CODE_ENUM = new InjectionToken('status.code.enum');
export const MAIN_SERVICE = new InjectionToken('main.service');